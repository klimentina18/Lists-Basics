factor = int(input())
count = int(input())

numbers_list = []
end = factor * count

for i in range(1, end + 1):   #(factor, end + 1, factor)
    if i % factor == 0:         #ohne /nach oben/
        numbers_list.append(i)

print(numbers_list)
